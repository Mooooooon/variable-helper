// 扩展的常量定义

export const extensionName = 'variable-helper';
export const extensionFolderPath = `scripts/extensions/third-party/${extensionName}`;

export const defaultSettings = {
  enabled: true,
  debug: false,
  conditionalEnabled: true,
};
